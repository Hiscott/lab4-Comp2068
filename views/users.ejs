<% include partials/header.ejs %>

    <h1><%= title %></h1>
    <ul>
        <% // loop through our users variable and display as a list
            for (var i = 0; i < users.length; i++) { %>
            <li><%= users[i] %></li>
        <% } %>
    </ul>

<% include partials/footer.ejs %>